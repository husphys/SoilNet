# SoilNet iPhone Telegram deployment patch

Copy this archive into the root of the existing SoilNet repository.

It adds an isolated deployment subsystem only. It does not contain model
weights or Telegram credentials and does not modify any frozen result.

Read `docs/IPHONE_TELEGRAM_DEMO.md` before enabling radio or pump output.
