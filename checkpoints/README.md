# Checkpoints

No checkpoint is selected merely by filename. See
`results/audit/checkpoint_inventory.csv` for SHA256 identities and
`checkpoint_selection_candidates.json` for the unapproved best/final-name
candidates. Only a minimal, lineage-verified set may later be added with Git
LFS.
